name: Download Linphone Wheel

on:
  workflow_dispatch:

jobs:
  download:
    runs-on: ubuntu-latest
    steps:
      - name: List available files
        run: |
          curl -s "https://download.linphone.org/releases/windows/sdk/" | grep -i "whl\|python\|cp31"
